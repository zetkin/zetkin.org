{% if ZETKIN_USE_TLS is defined %}
server {
  listen 80 default;
  server_name
    manual.{{ ZETKIN_DOMAIN }}
    www.{{ ZETKIN_DOMAIN }};

  location / {
    rewrite     ^ https://$host$request_uri? permanent;
  }
}
{% endif %}
